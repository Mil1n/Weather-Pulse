{
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check script.js && node --check sw.js && python3 -m json.tool manifest.json >/dev/null"
  },
  "devDependencies": {}
}
